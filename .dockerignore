# Build artifacts
llama.cpp/build*
llama.cpp/.github
*.o
*.a
.cache/
__pycache__/
*.pyc
*.log
.DS_Store
*.tar
*.deb

# Git metadata (history has no place in a build context)
.git
# Note: llama.cpp/.git is a *file* in submodule repos, not a dir — no need to exclude it

# Models and large files
models/
*.gguf
*.bin
*.safetensors

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# CI/CD and repo metadata (not needed for the build)
.github/
*.md
LICENSE
